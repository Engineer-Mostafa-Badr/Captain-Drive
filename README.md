# كابتن درايف لتوصيل الركاب(Captain-Drive)

تطبيق Captain Drive يربط بين العملاء والملاّك (الكباتن) في منصة واحدة مخصصة لرحلات الركاب، الحجز، ومتابعة الطلبات في الوقت الحقيقي. يدعم التطبيق شاشتين رئيسيتين (المستخدم/العميل والكابتن) مع تجربة سهلة، خرائط دقيقة، وإشعارات فورية تساعد في إنهاء الرحلة بكفاءة.

📱 **المميزات الأساسية**

- اختيار حساب الركاب أو الكابتن مع تدفقات دخول وتسجيل مختلفة (رمز OTP، إيميل، رقم هاتف).
- شاشة رئيسية للمستخدم توفر العروض، تفاصيل الطلبات، المحفظة، والمفضلة مع تبويب الدردشة لتواصل مباشر.
- لوحة كابتن متكاملة لعرض الطلبات، العروض، الرحلات الجارية، وتحديث حالة الوصول/الإنهاء.
- تتبع المواقع والخريطة باستخدام `Google Maps` و`geolocator` مع خيارات لإرسال الموقع الحالي للكابتن.
- إشعارات Firebase وواجهة تنبيهات تساعد في متابعة أي تغيير في حالة الرحلة أو وصول جديد.
- دعم ثنائي اللغة (العربية - الإنجليزية) مع ملفات ترجمة تحت `assets/lang`.
- إدارة الحالة عبر `BLoC` وتقسيم واضح بين البيانات، المنطق، والواجهة.

### 🏗️ البنية المعمارية

المشروع يعتمد معمارية منظمة تشرح حدود وظيفية لكل طبقة:

```
lib/
├── config/          # إعداد الثيمات، التوجيهات، الإعدادات العامة للمشروع
├── core/            # الخدمات المشتركة: الشبكة، التخزين، التوجيه، DI، الثيمات
├── business_logic/   # Cubits/BLoCs لكل ميزة (العروض، الخرائط، الكابتن... )
├── data/            # المستودعات، الخدمات الشبكية، وواجهات التواصل مع الخادم
├── features/        # كل ميزة مستقلة (auth، home، driver، splash، chat...)
├── screens/         # الشاشات والدالات التي تجمع ميزات متعددة في UI واحد
└── main.dart         # نقطة البداية، التهيئة، MultiBlocProvider، ضبط شاشة البداية
```

كل ميزة في `features/` تحتوي على تقسيمها الخاص بين واجهة المستخدم، حالات BOLO، والتوابع الشبكية إن وجدت. توجد مكونات متكررة في `core/components`، وملفات الإعداد في `config/` للمساعدة في التهيئة السريعة.

### 🧰 تقنيات وأدوات رئيسية

- Flutter 3.8+ مع دعم `null-safety`.
- إدارة الحالة: `flutter_bloc`, `bloc`, `dartz`.
- `dio` + `http` للتواصل، مع `shared_preferences` و`get_it` للحقن.
- Firebase: Core، Auth، Messaging، Storage، Firestore.
- الخرائط والموقع: `google_maps_flutter`, `geolocator`, `location`, `flutter_polyline_points`.
- وسائط حديثة: `camera`, `image_picker`, `video_player`.
- واجهة مستخدم: خطوط مخصصة، `carousel_slider`, `animated_text_kit`, `flutter_rating_stars`.
- الترجمة: ملفات JSON تحت `assets/lang` وتتحكم بها `LocalizationCubit`.

### ⚙️ التشغيل وبناء المشروع

```bash
# تنظيف المشروع
flutter clean

# تنزيل الحزم
flutter pub get

# تشغيل التطبيق (Android/iOS/web)
flutter run

# بناء APK مع تقنيات الحماية
flutter build apk --obfuscate --split-debug-info=build/app/outputs/symbols
```

### 📝 ملاحظات التطوير

- التنقل بين حساب الكابتن والراكب يتم عبر `features/auth/auth_selector`.
- كل Cubit في `business_logic` يربط شاشة واحدة أو أكثر بمجموعة أوامر محددة (تحديث العروض، إرسال الرسائل، تعديل الحالة..).
- الألوان والثوابت مخزنة في `core/theme` و`core/components/constants` لتوحيد المظهر.
- هناك دعم للصور والخطوط في `assets/images` و`assets/fonts` لتوفير واجهة غنية.
- يتم تهيئة خدمات الشبكة (Dio) والتخزين (SharedPreferences) قبل تشغيل `runApp`.

لأي إضافة جديدة، حافظ على الفصل بين `features`, `business_logic`, و`data`، واستفد من `core` للخدمات المشتركة.
